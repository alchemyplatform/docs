SUI_ADDRESS:
  type: string
  description: A 32-byte hexadecimal Sui address (e.g., 0x...).

MOVE_FUNCTION_ARG_TYPE:
  oneOf:
    - type: string
      enum: ["Pure"]
    - type: object
      properties:
        Object:
          type: string
          enum: ["ByImmutableReference", "ByMutableReference", "ByValue"]
