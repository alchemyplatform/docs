components:
  methods:
    getAccountInfo:
      name: getAccountInfo
      description: Returns all information associated with the account of provided Pubkey.
      params:
        - name: Pubkey
          required: true
          description: Pubkey of the account to query.
          schema:
            $ref: "./base-types.yaml#/components/schemas/Pubkey"
        - name: Configuration
          required: false
          description: Optional configuration object for additional settings.
          schema:
            $ref: "./account.yaml#/components/schemas/GetAccountInfoConfig"
      examples:
        - name: getAccountInfo example
          params:
            - name: Pubkey
              value: "GwsPP9HHhCvEQeu3HTFzsVL6DEtnnYw4ALEtA3fMBC9Q"
      result:
        name: Account information
        description: Returns details of the account including balance, ownership, and other relevant data.
        schema:
          $ref: "./account.yaml#/components/schemas/AccountInfo"

    getBalance:
      name: getBalance
      description: Returns the lamport balance of the account of the provided Pubkey.
      params:
        - name: Pubkey
          required: true
          description: Pubkey of the account to query.
          schema:
            $ref: "./base-types.yaml#/components/schemas/Pubkey"
        - name: Configuration
          required: false
          description: Optional configuration object containing additional settings.
          schema:
            $ref: "./account.yaml#/components/schemas/GetBalanceConfig"
      examples:
        - name: getBalance example
          params:
            - name: Pubkey
              value: "GwsPP9HHhCvEQeu3HTFzsVL6DEtnnYw4ALEtA3fMBC9Q"
      result:
        name: Lamport balance
        description: Returns the balance in lamports of the account queried.
        schema:
          $ref: "./account.yaml#/components/schemas/LamportBalance"

    getMultipleAccounts:
      name: getMultipleAccounts
      description: Returns the account information for a list of Pubkeys.
      params:
        - name: Pubkeys
          required: true
          description: An array of Pubkeys to query.
          schema:
            type: array
            items:
              $ref: "./base-types.yaml#/components/schemas/Pubkey"
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: "./account.yaml#/components/schemas/GetMultipleAccountsConfig"
      examples:
        - name: getMultipleAccounts example
          params:
            - name: Pubkeys
              value:
                - "GwsPP9HHhCvEQeu3HTFzsVL6DEtnnYw4ALEtA3fMBC9Q"
                - "EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v"
      result:
        name: Account information
        description: An array of JSON objects containing account details.
        schema:
          type: array
          items:
            $ref: "./account.yaml#/components/schemas/AccountInfo"

    getTokenAccountBalance:
      name: getTokenAccountBalance
      description: Returns the token balance of an SPL Token account.
      params:
        - name: Token account Pubkey
          required: true
          description: The Pubkey of the token account to query.
          schema:
            $ref: "./base-types.yaml#/components/schemas/Pubkey"
        - name: Configuration
          required: false
          description: Optional configuration object containing additional settings.
          schema:
            $ref: "./account.yaml#/components/schemas/GetTokenAccountBalanceConfig"
      examples:
        - name: getTokenAccountBalance example
          params:
            - name: Token account Pubkey
              value: "EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v"
      result:
        name: Token account balance
        description: The balance of the SPL Token account.
        schema:
          $ref: "./account.yaml#/components/schemas/TokenBalance"

    getTokenAccountsByOwner:
      name: getTokenAccountsByOwner
      description: Returns all SPL Token accounts owned by the specified token owner.
      params:
        - name: Token owner Pubkey
          required: true
          description: The Pubkey of the account owner to query.
          schema:
            $ref: "./base-types.yaml#/components/schemas/Pubkey"
        - name: Token filter
          required: false
          description: A filter object containing either the Mint Pubkey or the Token program Pubkey.
          schema:
            type: object
            properties:
              mint:
                $ref: "./base-types.yaml#/components/schemas/Pubkey"
                description: The Pubkey of the specific token Mint to limit accounts to.
              programId:
                $ref: "./base-types.yaml#/components/schemas/Pubkey"
                description: The Pubkey of the Token program that owns the accounts.
        - name: Configuration
          required: false
          description: Optional configuration object containing additional settings.
          schema:
            $ref: "./account.yaml#/components/schemas/GetTokenAccountsByOwnerConfig"
      examples:
        - name: getTokenAccountsByOwner example
          params:
            - name: Token owner Pubkey
              value: "GwsPP9HHhCvEQeu3HTFzsVL6DEtnnYw4ALEtA3fMBC9Q"
      result:
        name: Token accounts
        description: An array of JSON objects representing the token accounts owned by the specified Pubkey.
        schema:
          type: array
          items:
            $ref: "./account.yaml#/components/schemas/TokenAccount"

    getLargestAccounts:
      name: getLargestAccounts
      description: Returns the 20 largest accounts, by lamport balance.
      params:
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: "./account.yaml#/components/schemas/LargestAccountsConfig"
      result:
        name: Largest accounts
        description: An array of objects containing the address of the account and its lamport balance.
        schema:
          $ref: "./account.yaml#/components/schemas/LargestAccountsList"

    # Airdrop methods
    requestAirdrop:
      name: requestAirdrop
      description: Requests an airdrop of lamports to a Pubkey.
      params:
        - name: Pubkey
          required: true
          description: The Pubkey of the account to receive lamports.
          schema:
            $ref: "./base-types.yaml#/components/schemas/Pubkey"
        - name: Lamports
          required: true
          description: The number of lamports to airdrop, as a "u64".
          schema:
            type: integer
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: "./airdrop.yaml#/components/schemas/RequestAirdropConfig"
      examples:
        - name: requestAirdrop example
          params:
            - name: Pubkey
              value: "GwsPP9HHhCvEQeu3HTFzsVL6DEtnnYw4ALEtA3fMBC9Q"
            - name: Lamports
              value: 1000000000000000000
      result:
        name: Transaction signature
        description: Transaction Signature of the airdrop.
        schema:
          type: string

    # Block methods
    getBlock:
      name: getBlock
      description: Returns identity and transaction information about a confirmed block in the ledger.
      params:
        - name: slot
          required: true
          description: Slot number as a u64 integer.
          schema:
            type: integer
        - name: Configuration
          required: false
          description: Optional configuration object containing additional settings.
          schema:
            $ref: "./block.yaml#/components/schemas/GetBlockConfig"
      examples:
        - name: getBlock example
          params:
            - name: slot
              value: 237158054
            - name: Configuration
              value:
                encoding: "json"
                transactionDetails: "none"
      result:
        name: Block information
        description: Returns block information or null if the block is not confirmed.
        schema:
          $ref: "./block.yaml#/components/schemas/BlockInfo"

    getBlocks:
      name: getBlocks
      description: Returns a list of confirmed blocks between two slots.
      params:
        - name: start_slot
          required: true
          description: The starting slot.
          schema:
            type: integer
        - name: end_slot
          required: false
          description: The ending slot.
          schema:
            type: integer
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: "./block.yaml#/components/schemas/GetBlocksConfig"
      examples:
        - name: getBlocks (full example)
          params:
            - name: start_slot
              value: 237158000
            - name: end_slot
              value: 237158050
      result:
        name: List of confirmed blocks
        description: Returns an array of u64 integers listing confirmed blocks.
        schema:
          $ref: "./block.yaml#/components/schemas/SlotList"

    getBlocksWithLimit:
      name: getBlocksWithLimit
      description: Returns a list of confirmed blocks starting at the given slot.
      params:
        - name: start_slot
          required: true
          description: The starting slot.
          schema:
            type: integer
        - name: limit
          required: true
          description: The limit for the number of blocks to return.
          schema:
            type: integer
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: "./block.yaml#/components/schemas/GetBlocksConfig"
      examples:
        - name: getBlocksWithLimit (with configuration)
          params:
            - name: start_slot
              value: 237158000
            - name: limit
              value: 5
      result:
        name: List of confirmed blocks
        description: Returns an array of u64 integers listing confirmed blocks starting at the start_slot.
        schema:
          $ref: "./block.yaml#/components/schemas/SlotList"

    getBlockCommitment:
      name: getBlockCommitment
      description: Returns the commitment for a particular block.
      params:
        - name: block
          required: true
          description: Block number, identified by Slot.
          schema:
            type: integer
      examples:
        - name: getBlockCommitment example
          params:
            - name: block
              value: 237158054
      result:
        name: Block commitment
        description: Returns the commitment and total active stake for the block.
        schema:
          $ref: "./block.yaml#/components/schemas/GetBlockCommitmentResult"

    getBlockProduction:
      name: getBlockProduction
      description: Returns recent block production information from the current or previous epoch.
      params:
        - name: Configuration
          required: false
          description: Optional configuration object containing additional settings.
          schema:
            $ref: "./block.yaml#/components/schemas/GetBlockProductionConfig"
      examples:
        - name: getBlockProduction example
          params: []
      result:
        name: Block production information
        description: Returns block production information for the specified range.
        schema:
          $ref: "./block.yaml#/components/schemas/BlockProductionInfo"

    getBlockTime:
      name: getBlockTime
      description: Returns the estimated production time of a block.
      params:
        - name: block
          required: true
          description: Block number, identified by Slot.
          schema:
            $ref: "./block.yaml#/components/schemas/GetBlockTimeParams"
      examples:
        - name: getBlockTime example
          params:
            - name: block
              value: 237158054
      result:
        name: Estimated production time
        description: The estimated production time of the block as a Unix timestamp.
        schema:
          $ref: "./block.yaml#/components/schemas/EstimatedProductionTime"

    getFirstAvailableBlock:
      name: getFirstAvailableBlock
      description: Returns the slot of the lowest confirmed block that has not been purged from the ledger.
      params: []
      result:
        name: Slot
        description: The slot number of the lowest confirmed block that is still available in the ledger.
        schema:
          $ref: "./block.yaml#/components/schemas/FirstAvailableBlockSlot"

    getGenesisHash:
      name: getGenesisHash
      description: Returns the genesis hash.
      params: []
      result:
        name: Genesis hash
        description: The genesis hash as a base-58 encoded string.
        schema:
          $ref: "./block.yaml#/components/schemas/GenesisHash"

    getHighestSnapshotSlot:
      name: getHighestSnapshotSlot
      description: Returns the highest slot information that the node has snapshots for.
      params: []
      result:
        name: Snapshot slot information
        description: Returns a JSON object with the highest full snapshot slot.
        schema:
          $ref: "./block.yaml#/components/schemas/HighestSnapshotSlot"

    getMaxRetransmitSlot:
      name: getMaxRetransmitSlot
      description: Get the max slot seen from the retransmit stage.
      params: []
      result:
        name: Slot number
        description: The maximum slot number seen from the retransmit stage.
        schema:
          $ref: "./block.yaml#/components/schemas/MaxSlotResult"

    getMaxShredInsertSlot:
      name: getMaxShredInsertSlot
      description: Get the max slot seen from after shred insert.
      params: []
      result:
        name: Slot number
        description: The maximum slot number seen after shred insert.
        schema:
          $ref: "./block.yaml#/components/schemas/MaxSlotResult"

    getBlockHeight:
      name: getBlockHeight
      description: Returns the current block height of the node.
      params:
        - name: Configuration
          required: false
          description: Optional configuration object containing additional request parameters.
          schema:
            $ref: "./block.yaml#/components/schemas/GetBlockHeightConfig"
      result:
        name: Block height
        description: The current block height, as a u64 integer.
        schema:
          $ref: "./block.yaml#/components/schemas/BlockHeight"

    getLatestBlockhash:
      name: getLatestBlockhash
      description: Returns the latest blockhash.
      params:
        - name: Configuration
          required: false
          description: A configuration object with optional fields for specifying commitment and minimum context slot.
          schema:
            $ref: "./block.yaml#/components/schemas/GetLatestBlockhashConfig"
      result:
        name: Latest Blockhash
        description: RpcResponse object with blockhash information.
        schema:
          $ref: "./block.yaml#/components/schemas/LatestBlockhash"

    isBlockhashValid:
      name: isBlockhashValid
      description: Returns whether a blockhash is still valid or not.
      params:
        - name: blockhash
          required: true
          description: The blockhash of the block to evaluate.
          schema:
            type: string
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: "./block.yaml#/components/schemas/IsBlockhashValidParams"
      result:
        name: Validity
        description: Indicates if the blockhash is still valid.
        schema:
          $ref: "./block.yaml#/components/schemas/IsBlockhashValidResult"

    # Cluster Program methods
    getClusterNodes:
      name: getClusterNodes
      description: Returns information about all the nodes participating in the cluster.
      params: []
      result:
        name: List of nodes
        description: Returns an array of JSON objects, each containing information about a node in the cluster.
        schema:
          $ref: "./cluster-program.yaml#/components/schemas/ClusterNodesList"

    getHealth:
      name: getHealth
      description: Returns the current health of the node.
      params: []
      result:
        name: Health status
        description: If the node is healthy, the result is "ok".
        schema:
          $ref: "./cluster-program.yaml#/components/schemas/HealthStatus"

    getIdentity:
      name: getIdentity
      description: Returns the identity pubkey for the current node.
      params: []
      result:
        name: Identity pubkey
        description: The identity pubkey of the current node.
        schema:
          $ref: "./cluster-program.yaml#/components/schemas/NodeIdentity"

    getSlotLeader:
      name: getSlotLeader
      description: Returns the current slot leader.
      params:
        - name: Configuration
          required: false
          description: Optional configuration object containing additional settings.
          schema:
            $ref: "./state.yaml#/components/schemas/SlotConfig"
      result:
        name: Slot leader
        description: The node identity Pubkey as a base-58 encoded string.
        schema:
          $ref: "./state.yaml#/components/schemas/SlotLeader"

    getSlotLeaders:
      name: getSlotLeaders
      description: Returns the slot leaders for a given slot range.
      params:
        - name: Start slot
          required: true
          description: The starting slot as a u64 integer.
          schema:
            type: integer
        - name: Limit
          required: true
          description: The number of slot leaders to return, between 1 and 5,000.
          schema:
            type: integer
      result:
        name: Slot leaders
        description: An array of node identity public keys as base-58 encoded strings.
        schema:
          $ref: "./state.yaml#/components/schemas/SlotLeaders"

    getVoteAccounts:
      name: getVoteAccounts
      description: Returns the account info and associated stake for all voting accounts in the current bank.
      params:
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: "./cluster-program.yaml#/components/schemas/GetVoteAccountsConfig"
      result:
        name: Vote accounts
        description: Information about current and delinquent vote accounts.
        schema:
          $ref: "./cluster-program.yaml#/components/schemas/VoteAccounts"

    getProgramAccounts:
      name: getProgramAccounts
      description: Returns all accounts owned by the provided program Pubkey.
      params:
        - name: Pubkey
          required: true
          description: The Pubkey of the program.
          schema:
            $ref: "./base-types.yaml#/components/schemas/Pubkey"
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: "./cluster-program.yaml#/components/schemas/GetProgramAccountsConfig"
      result:
        name: Program accounts
        description: An array of JSON objects containing program account details.
        schema:
          type: array
          items:
            $ref: "./account.yaml#/components/schemas/ProgramAccount"

    getLeaderSchedule:
      name: getLeaderSchedule
      description: Returns the leader schedule for a given epoch.
      params:
        - name: Slot
          required: false
          description: The slot to fetch the leader schedule for. If unspecified, returns the leader schedule for the current epoch.
          schema:
            type: integer
            format: uint64
        - name: Configuration
          required: false
          description: Optional configuration object containing additional settings.
          schema:
            type: object
            properties:
              commitment:
                type: string
                description: The commitment level to use for the request.
              identity:
                type: string
                description: Only return results for this validator identity (base-58 encoded).
      examples:
        - name: getLeaderSchedule example
          params:
            - name: Slot
              value: null
            - name: Configuration
              value:
                commitment: "processed"
                identity: "dv2eQHeP4RFrJZ6UeiZWoc3XTtmtZCUKxxCApCDcRNV"

    # Fee methods
    getFeeForMessage:
      name: getFeeForMessage
      description: Get the fee the network will charge for a particular Message.
      params:
        - name: Message
          required: true
          description: Base-64 encoded Message.
          schema:
            $ref: "./fee.yaml#/components/schemas/FeeForMessageParams"
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: "./fee.yaml#/components/schemas/FeeForMessageConfig"
      result:
        name: Fee for the message
        description: The fee corresponding to the message at the specified blockhash.
        schema:
          $ref: "./fee.yaml#/components/schemas/FeeForMessageResult"

    getMinimumBalanceForRentExemption:
      name: getMinimumBalanceForRentExemption
      description: Returns the minimum balance required to make an account rent exempt.
      params:
        - name: Account data length
          required: true
          description: The account's data length in bytes.
          schema:
            $ref: "./fee.yaml#/components/schemas/MinimumBalanceForRentExemptionParams"
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: "./fee.yaml#/components/schemas/MinimumBalanceForRentExemptionConfig"
      result:
        name: Minimum balance for rent exemption
        description: The minimum number of lamports required in the account to remain rent-free.
        schema:
          $ref: "./fee.yaml#/components/schemas/MinimumBalanceForRentExemptionResult"

    getRecentPrioritizationFees:
      name: getRecentPrioritizationFees
      description: Returns a list of prioritization fees from recent blocks.
      params:
        - name: Account addresses
          required: false
          description: An array of up to 128 account addresses. If provided, the response will reflect a fee to land a transaction locking all of the provided accounts as writable.
          schema:
            type: array
            items:
              $ref: "./base-types.yaml#/components/schemas/Pubkey"
      result:
        name: Prioritization fees
        description: An array of prioritization fees observed in recent blocks.
        schema:
          $ref: "./fee.yaml#/components/schemas/GetRecentPrioritizationFeesResult"

    getPriorityFeeEstimate:
      name: getPriorityFeeEstimate
      description: Calculate optimal priority fee recommendations for Solana transactions based on real-time network conditions.
      params:
        - name: Configuration
          required: true
          description: Single params object. Top-level keys are transaction, accountKeys, and options (nested). Put priorityLevel, recommended, etc. inside options.
          schema:
            $ref: "./fee.yaml#/components/schemas/GetPriorityFeeEstimateParams"
      examples:
        - name: getPriorityFeeEstimate with transaction
          params:
            - name: Configuration
              value:
                transaction: ""
                options:
                  priorityLevel: "High"
      result:
        name: Priority fee estimate
        description: Optimal priority fee estimate and optional fee levels for different priority tiers.
        schema:
          $ref: "./fee.yaml#/components/schemas/GetPriorityFeeEstimateResult"

    # Inflation methods
    getInflationGovernor:
      name: getInflationGovernor
      description: Returns the current inflation governor.
      params:
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: "./state.yaml#/components/schemas/GetEpochInfoConfig"
      result:
        name: Inflation governor details
        description: Returns a JSON object containing the inflation governor parameters.
        schema:
          $ref: "./inflation.yaml#/components/schemas/InflationGovernor"

    getInflationRate:
      name: getInflationRate
      description: Returns the specific inflation values for the current epoch.
      params: []
      result:
        name: Inflation rate details
        description: Returns a JSON object containing the inflation values for the current epoch.
        schema:
          $ref: "./inflation.yaml#/components/schemas/InflationRate"

    getInflationReward:
      name: getInflationReward
      description: Returns the inflation or staking reward for a list of addresses for a specified epoch.
      params:
        - name: Addresses
          required: true
          description: An array of addresses to query.
          schema:
            $ref: "./inflation.yaml#/components/schemas/InflationRewardParams"
      result:
        name: Inflation reward details
        description: Returns a JSON array with reward details for each address.
        schema:
          type: array
          items:
            $ref: "./inflation.yaml#/components/schemas/InflationReward"

    # Performance methods
    getRecentPerformanceSamples:
      name: getRecentPerformanceSamples
      description: Returns a list of recent performance samples, in reverse slot order.
      params:
        - name: limit
          required: false
          description: The number of samples to return.
          schema:
            type: integer
      result:
        name: Performance samples
        description: An array of performance sample data.
        schema:
          type: array
          items:
            $ref: "./performance.yaml#/components/schemas/PerformanceSample"

    # State methods
    getEpochInfo:
      name: getEpochInfo
      description: Returns information about the current epoch.
      params:
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: "./state.yaml#/components/schemas/GetEpochInfoConfig"
      result:
        name: Epoch information
        description: Returns an object containing information about the current epoch.
        schema:
          $ref: "./state.yaml#/components/schemas/EpochInfo"

    getEpochSchedule:
      name: getEpochSchedule
      description: Returns the epoch schedule information from the cluster's genesis config.
      params: []
      result:
        name: Epoch schedule information
        description: Returns an object containing information about the epoch schedule.
        schema:
          $ref: "./state.yaml#/components/schemas/EpochSchedule"

    getSlot:
      name: getSlot
      description: Returns the slot that has reached the given or default commitment level.
      params:
        - name: Configuration
          required: false
          description: Optional configuration object containing additional settings.
          schema:
            $ref: "./state.yaml#/components/schemas/SlotConfig"
      result:
        name: Current slot
        description: The current slot at the specified commitment level.
        schema:
          type: integer

    minimumLedgerSlot:
      name: minimumLedgerSlot
      description: Returns the lowest slot that the node has information about in its ledger.
      params: []
      result:
        name: Minimum ledger slot
        description: The lowest slot number the node has information about.
        schema:
          $ref: "./state.yaml#/components/schemas/MinimumLedgerSlot"

    getVersion:
      name: getVersion
      description: Returns the current Solana version running on the node.
      params: []
      result:
        name: Version details
        description: The current Solana version running on the node.
        schema:
          $ref: "./state.yaml#/components/schemas/VersionInfo"

    # Supply methods
    getSupply:
      name: getSupply
      description: Returns information about the current supply of lamports.
      params:
        - name: Configuration
          required: false
          description: Optional configuration object containing additional settings.
          schema:
            $ref: "./supply.yaml#/components/schemas/GetSupplyConfig"
      result:
        name: Supply information
        description: An RpcResponse JSON object containing the current supply information.
        schema:
          $ref: "./supply.yaml#/components/schemas/SupplyInfo"

    getTokenSupply:
      name: getTokenSupply
      description: Returns the total supply of an SPL Token type.
      params:
        - name: Token Mint Pubkey
          required: true
          description: The Pubkey of the token Mint to query.
          schema:
            $ref: "./base-types.yaml#/components/schemas/Pubkey"
        - name: Configuration
          required: false
          description: Optional configuration object containing additional settings.
          schema:
            $ref: "./supply.yaml#/components/schemas/GetTokenSupplyConfig"
      result:
        name: Token supply
        description: The total supply of the specified SPL Token.
        schema:
          $ref: "./account.yaml#/components/schemas/TokenBalance"

    # Transaction methods
    sendTransaction:
      name: sendTransaction
      description: Submits a signed transaction to the cluster for processing.
      params:
        - name: Transaction
          required: true
          description: Fully-signed Transaction, as encoded string.
          schema:
            type: string
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: "./transaction.yaml#/components/schemas/SendTransactionConfig"
      result:
        name: Transaction signature
        description: The first signature in the transaction, used as the transaction ID.
        schema:
          $ref: "./transaction.yaml#/components/schemas/SendTransactionResult"

    simulateTransaction:
      name: simulateTransaction
      description: Simulates sending a transaction.
      params:
        - name: Transaction
          required: true
          description: The transaction to simulate, as an encoded string.
          schema:
            type: string
        - name: Configuration
          required: false
          description: Optional configuration object containing additional options.
          schema:
            $ref: "./transaction.yaml#/components/schemas/SimulateTransactionConfig"
      examples:
        - name: simulateTransaction example
          params:
            - name: Transaction
              value: ""
            - name: Configuration
              value:
                encoding: "base64"
          result:
            name: Simulated transaction result
            value:
              err: null
              logs:
                - "Program TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb invoke [1]"
                - "Program log: Instruction: Transfer"
                - "Program TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb consumed 1714 of 200000 compute units"
                - "Program TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb success"
              accounts: null
              unitsConsumed: 1714
              returnData: null
              innerInstructions: null
      result:
        name: Simulated transaction result
        description: The result of simulating the transaction.
        schema:
          $ref: "./transaction.yaml#/components/schemas/SimulatedTransactionResult"

    simulateBundle:
      name: simulateBundle
      description: Simulates sending a Jito bundle of transactions.
      params:
        - name: Bundle
          required: true
          description: Encoded, serialized transactions to simulate in a bundle.
          schema:
            $ref: "./transaction.yaml#/components/schemas/SimulateBundleParams"
        - name: Configuration
          required: false
          description: Optional configuration object containing additional options.
          schema:
            $ref: "./transaction.yaml#/components/schemas/SimulateBundleConfig"
      result:
        name: Simulated bundle result
        description: The result of simulating the bundle.
        schema:
          $ref: "./transaction.yaml#/components/schemas/SimulatedBundleResult"

    getSignaturesForAddress:
      name: getSignaturesForAddress
      description: Returns signatures for confirmed transactions that include the given address.
      params:
        - name: Account address
          required: true
          description: The account address.
          schema:
            $ref: "./base-types.yaml#/components/schemas/Pubkey"
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: "./transaction.yaml#/components/schemas/GetSignaturesForAddressConfig"
      result:
        name: Transaction signatures
        description: An array of objects containing transaction signature information.
        schema:
          type: array
          items:
            $ref: "./transaction.yaml#/components/schemas/SignatureInfo"

    getSignatureStatuses:
      name: getSignatureStatuses
      description: Returns the statuses of a list of transaction signatures.
      params:
        - name: Signatures
          required: true
          description: An array of transaction signatures to confirm.
          schema:
            type: array
            items:
              type: string
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: "./transaction.yaml#/components/schemas/GetSignatureStatusesConfig"
      result:
        name: Transaction statuses
        description: An array containing the status of each transaction signature.
        schema:
          type: array
          items:
            $ref: "./transaction.yaml#/components/schemas/TransactionStatus"

    getTransaction:
      name: getTransaction
      description: Returns transaction details for a confirmed transaction.
      params:
        - name: Transaction signature
          required: true
          description: Transaction signature as a base-58 encoded string.
          schema:
            type: string
        - name: Configuration
          required: false
          description: Optional configuration object containing additional settings.
          schema:
            $ref: "./transaction.yaml#/components/schemas/GetTransactionConfig"
      result:
        name: Transaction details
        description: The details of a confirmed transaction.
        schema:
          $ref: "./transaction.yaml#/components/schemas/TransactionDetails"
    getTokenAccountsByDelegate:
      name: getTokenAccountsByDelegate
      description: Returns all SPL Token accounts delegated to the provided account.
      params:
        - name: Delegate Pubkey
          required: true
          description: The Pubkey of the account delegate to query.
          schema:
            $ref: ./base-types.yaml#/components/schemas/Pubkey
        - name: Token filter
          required: false
          description: >-
            A filter object containing either the Mint Pubkey or the Token program
            Pubkey.
          schema:
            type: object
            properties:
              mint:
                $ref: ./base-types.yaml#/components/schemas/Pubkey
                description: The Pubkey of the specific token Mint to limit accounts to.
              programId:
                $ref: ./base-types.yaml#/components/schemas/Pubkey
                description: The Pubkey of the Token program that owns the accounts.
        - name: Configuration
          required: false
          description: Optional configuration object containing additional settings.
          schema:
            $ref: >-
              ./account.yaml#/components/schemas/GetTokenAccountsByDelegateConfig
      examples:
        - name: getTokenAccountsByDelegate example
          params:
            - name: Delegate Pubkey
              value: 4Nd1mBQtrMJVYVfKf2PJy9NZUZdTAsp7D4xWLs4gDB4T
            - name: Token filter
              value:
                programId: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
      result:
        name: Token accounts
        description: >-
          An array of JSON objects representing the token accounts delegated to
          the specified Pubkey.
        schema:
          type: array
          items:
            $ref: ./account.yaml#/components/schemas/TokenAccount
    getTokenLargestAccounts:
      name: getTokenLargestAccounts
      description: Returns the 20 largest accounts of a particular SPL Token type.
      params:
        - name: Token Mint Pubkey
          required: true
          description: The Pubkey of the token Mint to query.
          schema:
            $ref: ./base-types.yaml#/components/schemas/Pubkey
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: >-
              ./account.yaml#/components/schemas/GetTokenLargestAccountsConfig
      examples:
        - name: getTokenLargestAccounts example
          params:
            - name: Token Mint Pubkey
              value: EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v
      result:
        name: Largest token accounts
        description: An array of the 20 largest accounts of the specified token type.
        schema:
          $ref: ./account.yaml#/components/schemas/TokenLargestAccountsList
    getStakeActivation:
      name: getStakeActivation
      description: Returns epoch activation information for a stake account.
      params:
        - name: Stake account Pubkey
          required: true
          description: Pubkey of the stake account to query.
          schema:
            $ref: ./base-types.yaml#/components/schemas/Pubkey
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: ./account.yaml#/components/schemas/StakeActivationConfig
      examples:
        - name: getStakeActivation example
          params:
            - name: Stake account Pubkey
              value: CYRJWqiSjLitBAcRxPvWpgX3s5TvmN2SuRY3eEYypFvT
      result:
        name: Stake activation info
        description: Returns epoch activation information for the stake account.
        schema:
          $ref: ./account.yaml#/components/schemas/StakeActivation
    getTransactionCount:
      name: getTransactionCount
      description: Returns the current transaction count from the ledger.
      params:
        - name: Configuration
          required: false
          description: Optional configuration object.
          schema:
            $ref: >-
              ./state.yaml#/components/schemas/GetTransactionCountConfig
      examples:
        - name: getTransactionCount example
          params: []
      result:
        name: Transaction count
        description: The current transaction count from the ledger.
        schema:
          $ref: ./state.yaml#/components/schemas/TransactionCount
    getTransactionsForAddress:
      name: getTransactionsForAddress
      description: >-
        Returns transaction history for a given address with advanced
        filtering, bidirectional sorting, and pagination. Combines the
        functionality of `getSignaturesForAddress` and `getTransaction` into a
        single call, returning up to 1,000 signatures or up to 100 full
        transactions per request.
      params:
        - name: Account address
          required: true
          description: Base-58 encoded public key of the account to query transaction history for.
          schema:
            $ref: ./base-types.yaml#/components/schemas/Pubkey
        - name: Configuration
          required: false
          description: >-
            Optional configuration object containing transaction detail level,
            sort order, limit, pagination token, commitment, encoding, and
            filters (slot, blockTime, signature, status, tokenAccounts).
          schema:
            $ref: ./transaction.yaml#/components/schemas/GetTransactionsForAddressConfig
      examples:
        - name: getTransactionsForAddress example
          params:
            - name: Account address
              value: "86xCnPeV69n6t3DnyGvkKobf9FdN2H9oiVDdaMpo2MMY"
            - name: Configuration
              value:
                transactionDetails: "signatures"
                sortOrder: "desc"
                limit: 100
                filters:
                  status: "succeeded"
                  tokenAccounts: "balanceChanged"
      result:
        name: Transaction history
        description: >-
          A paginated list of transactions for the address. Each entry
          includes the slot, transaction index, and either a signature plus
          metadata (when `transactionDetails` is `signatures`) or the full
          transaction and meta objects (when `transactionDetails` is `full`).
          A `paginationToken` is returned when more results are available.
        schema:
          $ref: ./transaction.yaml#/components/schemas/GetTransactionsForAddressResult

    # ZK Compression methods (Photon indexer)
    getCompressedAccount:
      name: getCompressedAccount
      description: Returns the compressed account with the given address or hash. Provide either `address` or `hash`.
      paramStructure: by-name
      params:
        - name: address
          required: false
          description: Pubkey of the compressed account to query.
          schema:
            oneOf:
              - $ref: ./base-types.yaml#/components/schemas/Pubkey
              - type: "null"
        - name: hash
          required: false
          description: Hash of the compressed account to query.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Hash
              - type: "null"
      examples:
        - name: getCompressedAccount example
          params:
            - name: hash
              value: "3KC9cKQhhzEPjwuyQaohM6SGaZu1ukKRZw2QRgc1sYVL"
      result:
        name: Compressed account
        description: The compressed account data with context information.
        schema:
          $ref: ./compression.yaml#/components/schemas/AccountResult

    getCompressedAccountProof:
      name: getCompressedAccountProof
      description: Returns a merkle proof for the compressed account with the given hash.
      params:
        - name: hash
          required: true
          description: Hash of the compressed account.
          schema:
            $ref: ./compression.yaml#/components/schemas/Hash
      examples:
        - name: getCompressedAccountProof example
          params:
            - name: hash
              value: "3KC9cKQhhzEPjwuyQaohM6SGaZu1ukKRZw2QRgc1sYVL"
      result:
        name: Compressed account proof
        description: The merkle proof for the compressed account.
        schema:
          $ref: ./compression.yaml#/components/schemas/AccountProofResult

    getCompressedAccountsByOwner:
      name: getCompressedAccountsByOwner
      description: Returns the compressed accounts owned by the given Pubkey.
      paramStructure: by-name
      params:
        - name: owner
          required: true
          description: Pubkey of the owner whose compressed accounts to query.
          schema:
            $ref: ./base-types.yaml#/components/schemas/Pubkey
        - name: cursor
          required: false
          description: Pagination cursor returned by a previous response.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Hash
              - type: "null"
        - name: dataSlice
          required: false
          description: Optional account data slice to return.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/DataSlice
              - type: "null"
        - name: filters
          required: false
          description: Optional filters to apply to the account data.
          schema:
            type: array
            items:
              $ref: ./compression.yaml#/components/schemas/FilterSelector
        - name: limit
          required: false
          description: Maximum number of accounts to return.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Limit
              - type: "null"
      examples:
        - name: getCompressedAccountsByOwner example
          params:
            - name: owner
              value: "11111112cMQwSC9qirWGjZM6gLGwW69X22mqwLLGP"
      result:
        name: Compressed accounts
        description: A paginated list of compressed accounts owned by the given Pubkey.
        schema:
          $ref: ./compression.yaml#/components/schemas/PaginatedAccountListResult

    getCompressedBalance:
      name: getCompressedBalance
      description: Returns the balance for the compressed account with the given address or hash. Provide either `address` or `hash`.
      paramStructure: by-name
      params:
        - name: address
          required: false
          description: Pubkey of the compressed account to query.
          schema:
            oneOf:
              - $ref: ./base-types.yaml#/components/schemas/Pubkey
              - type: "null"
        - name: hash
          required: false
          description: Hash of the compressed account to query.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Hash
              - type: "null"
      examples:
        - name: getCompressedBalance example
          params:
            - name: hash
              value: "3KC9cKQhhzEPjwuyQaohM6SGaZu1ukKRZw2QRgc1sYVL"
      result:
        name: Compressed balance
        description: The lamport balance of the compressed account.
        schema:
          $ref: ./compression.yaml#/components/schemas/UnsignedIntegerResult

    getCompressedBalanceByOwner:
      name: getCompressedBalanceByOwner
      description: Returns the total compressed lamport balance owned by the given Pubkey.
      params:
        - name: owner
          required: true
          description: The Pubkey whose compressed balance to query.
          schema:
            $ref: ./base-types.yaml#/components/schemas/Pubkey
      examples:
        - name: getCompressedBalanceByOwner example
          params:
            - name: owner
              value: "11111112cMQwSC9qirWGjZM6gLGwW69X22mqwLLGP"
      result:
        name: Compressed balance
        description: The total compressed lamport balance for the owner.
        schema:
          $ref: ./compression.yaml#/components/schemas/UnsignedIntegerResult

    getCompressedMintTokenHolders:
      name: getCompressedMintTokenHolders
      description: Returns owners of compressed token accounts for the given mint, ranked by balance.
      paramStructure: by-name
      params:
        - name: mint
          required: true
          description: Pubkey of the mint whose token holders to query.
          schema:
            $ref: ./base-types.yaml#/components/schemas/Pubkey
        - name: cursor
          required: false
          description: Pagination cursor returned by a previous response.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Base58String
              - type: "null"
        - name: limit
          required: false
          description: Maximum number of holders to return.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Limit
              - type: "null"
      examples:
        - name: getCompressedMintTokenHolders example
          params:
            - name: mint
              value: "11111112cMQwSC9qirWGjZM6gLGwW69X22mqwLLGP"
      result:
        name: Token holders
        description: A paginated list of owner balances for the given mint.
        schema:
          $ref: ./compression.yaml#/components/schemas/OwnerBalanceListResult

    getCompressedTokenAccountBalance:
      name: getCompressedTokenAccountBalance
      description: Returns the balance for the compressed token account with the given address or hash. Provide either `address` or `hash`.
      paramStructure: by-name
      params:
        - name: address
          required: false
          description: Pubkey of the compressed token account to query.
          schema:
            oneOf:
              - $ref: ./base-types.yaml#/components/schemas/Pubkey
              - type: "null"
        - name: hash
          required: false
          description: Hash of the compressed token account to query.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Hash
              - type: "null"
      examples:
        - name: getCompressedTokenAccountBalance example
          params:
            - name: hash
              value: "3KC9cKQhhzEPjwuyQaohM6SGaZu1ukKRZw2QRgc1sYVL"
      result:
        name: Token account balance
        description: The token balance of the compressed token account.
        schema:
          $ref: ./compression.yaml#/components/schemas/TokenAccountBalanceResult

    getCompressedTokenAccountsByDelegate:
      name: getCompressedTokenAccountsByDelegate
      description: Returns the compressed token accounts delegated to the given Pubkey.
      paramStructure: by-name
      params:
        - name: delegate
          required: true
          description: Pubkey of the delegate.
          schema:
            $ref: ./base-types.yaml#/components/schemas/Pubkey
        - name: mint
          required: false
          description: Optional mint Pubkey to filter accounts by.
          schema:
            oneOf:
              - $ref: ./base-types.yaml#/components/schemas/Pubkey
              - type: "null"
        - name: cursor
          required: false
          description: Pagination cursor returned by a previous response.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Base58String
              - type: "null"
        - name: limit
          required: false
          description: Maximum number of accounts to return.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Limit
              - type: "null"
      examples:
        - name: getCompressedTokenAccountsByDelegate example
          params:
            - name: delegate
              value: "11111112cMQwSC9qirWGjZM6gLGwW69X22mqwLLGP"
      result:
        name: Compressed token accounts
        description: A paginated list of compressed token accounts delegated to the given Pubkey.
        schema:
          $ref: ./compression.yaml#/components/schemas/TokenAccountListResult

    getCompressedTokenAccountsByOwner:
      name: getCompressedTokenAccountsByOwner
      description: Returns the compressed token accounts owned by the given Pubkey.
      paramStructure: by-name
      params:
        - name: owner
          required: true
          description: Pubkey of the owner whose compressed token accounts to query.
          schema:
            $ref: ./base-types.yaml#/components/schemas/Pubkey
        - name: mint
          required: false
          description: Optional mint Pubkey to filter accounts by.
          schema:
            oneOf:
              - $ref: ./base-types.yaml#/components/schemas/Pubkey
              - type: "null"
        - name: cursor
          required: false
          description: Pagination cursor returned by a previous response.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Base58String
              - type: "null"
        - name: limit
          required: false
          description: Maximum number of accounts to return.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Limit
              - type: "null"
      examples:
        - name: getCompressedTokenAccountsByOwner example
          params:
            - name: owner
              value: "11111112cMQwSC9qirWGjZM6gLGwW69X22mqwLLGP"
      result:
        name: Compressed token accounts
        description: A paginated list of compressed token accounts owned by the given Pubkey.
        schema:
          $ref: ./compression.yaml#/components/schemas/TokenAccountListResult

    getCompressedTokenBalancesByOwner:
      name: getCompressedTokenBalancesByOwner
      description: Returns the compressed token balances for the given Pubkey.
      paramStructure: by-name
      params:
        - name: owner
          required: true
          description: Pubkey of the owner whose compressed token balances to query.
          schema:
            $ref: ./base-types.yaml#/components/schemas/Pubkey
        - name: mint
          required: false
          description: Optional mint Pubkey to filter balances by.
          schema:
            oneOf:
              - $ref: ./base-types.yaml#/components/schemas/Pubkey
              - type: "null"
        - name: cursor
          required: false
          description: Pagination cursor returned by a previous response.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Base58String
              - type: "null"
        - name: limit
          required: false
          description: Maximum number of balances to return.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Limit
              - type: "null"
      examples:
        - name: getCompressedTokenBalancesByOwner example
          params:
            - name: owner
              value: "11111112cMQwSC9qirWGjZM6gLGwW69X22mqwLLGP"
      result:
        name: Compressed token balances
        description: A paginated list of compressed token balances for the owner.
        schema:
          $ref: ./compression.yaml#/components/schemas/TokenBalanceListResult

    getCompressedTokenBalancesByOwnerV2:
      name: getCompressedTokenBalancesByOwnerV2
      description: Returns the compressed token balances for the given Pubkey.
      paramStructure: by-name
      params:
        - name: owner
          required: true
          description: Pubkey of the owner whose compressed token balances to query.
          schema:
            $ref: ./base-types.yaml#/components/schemas/Pubkey
        - name: mint
          required: false
          description: Optional mint Pubkey to filter balances by.
          schema:
            oneOf:
              - $ref: ./base-types.yaml#/components/schemas/Pubkey
              - type: "null"
        - name: cursor
          required: false
          description: Pagination cursor returned by a previous response.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Base58String
              - type: "null"
        - name: limit
          required: false
          description: Maximum number of balances to return.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Limit
              - type: "null"
      examples:
        - name: getCompressedTokenBalancesByOwnerV2 example
          params:
            - name: owner
              value: "11111112cMQwSC9qirWGjZM6gLGwW69X22mqwLLGP"
      result:
        name: Compressed token balances
        description: A paginated list of compressed token balances for the owner.
        schema:
          $ref: ./compression.yaml#/components/schemas/TokenBalanceListV2Result

    getCompressionSignaturesForAccount:
      name: getCompressionSignaturesForAccount
      description: Returns the transaction signatures that closed or opened the compressed account with the given hash.
      params:
        - name: hash
          required: true
          description: Hash of the compressed account.
          schema:
            $ref: ./compression.yaml#/components/schemas/Hash
      examples:
        - name: getCompressionSignaturesForAccount example
          params:
            - name: hash
              value: "3KC9cKQhhzEPjwuyQaohM6SGaZu1ukKRZw2QRgc1sYVL"
      result:
        name: Signatures
        description: The transaction signatures that touched the compressed account.
        schema:
          $ref: ./compression.yaml#/components/schemas/SignatureInfoListResult

    getCompressionSignaturesForAddress:
      name: getCompressionSignaturesForAddress
      description: Returns the transaction signatures that closed or opened a compressed account at the given address.
      paramStructure: by-name
      params:
        - name: address
          required: true
          description: Pubkey of the compressed account address.
          schema:
            $ref: ./base-types.yaml#/components/schemas/Pubkey
        - name: cursor
          required: false
          description: Pagination cursor returned by a previous response.
          schema:
            oneOf:
              - type: string
              - type: "null"
        - name: limit
          required: false
          description: Maximum number of signatures to return.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Limit
              - type: "null"
      examples:
        - name: getCompressionSignaturesForAddress example
          params:
            - name: address
              value: "11111112cMQwSC9qirWGjZM6gLGwW69X22mqwLLGP"
      result:
        name: Signatures
        description: A paginated list of transaction signatures.
        schema:
          $ref: ./compression.yaml#/components/schemas/PaginatedSignatureInfoListResult

    getCompressionSignaturesForOwner:
      name: getCompressionSignaturesForOwner
      description: Returns the transaction signatures involving compressed accounts owned by the given Pubkey.
      paramStructure: by-name
      params:
        - name: owner
          required: true
          description: Pubkey of the owner whose compression-related signatures to query.
          schema:
            $ref: ./base-types.yaml#/components/schemas/Pubkey
        - name: cursor
          required: false
          description: Pagination cursor returned by a previous response.
          schema:
            oneOf:
              - type: string
              - type: "null"
        - name: limit
          required: false
          description: Maximum number of signatures to return.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Limit
              - type: "null"
      examples:
        - name: getCompressionSignaturesForOwner example
          params:
            - name: owner
              value: "11111112cMQwSC9qirWGjZM6gLGwW69X22mqwLLGP"
      result:
        name: Signatures
        description: A paginated list of transaction signatures.
        schema:
          $ref: ./compression.yaml#/components/schemas/PaginatedSignatureInfoListResult

    getCompressionSignaturesForTokenOwner:
      name: getCompressionSignaturesForTokenOwner
      description: Returns the transaction signatures involving compressed token accounts owned by the given Pubkey.
      paramStructure: by-name
      params:
        - name: owner
          required: true
          description: Pubkey of the owner whose compressed token signatures to query.
          schema:
            $ref: ./base-types.yaml#/components/schemas/Pubkey
        - name: cursor
          required: false
          description: Pagination cursor returned by a previous response.
          schema:
            oneOf:
              - type: string
              - type: "null"
        - name: limit
          required: false
          description: Maximum number of signatures to return.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Limit
              - type: "null"
      examples:
        - name: getCompressionSignaturesForTokenOwner example
          params:
            - name: owner
              value: "11111112cMQwSC9qirWGjZM6gLGwW69X22mqwLLGP"
      result:
        name: Signatures
        description: A paginated list of transaction signatures.
        schema:
          $ref: ./compression.yaml#/components/schemas/PaginatedSignatureInfoListResult

    getIndexerHealth:
      name: getIndexerHealth
      description: Returns the health status of the compression indexer.
      params: []
      examples:
        - name: getIndexerHealth example
          params: []
      result:
        name: Health status
        description: Returns `ok` when the indexer is healthy.
        schema:
          $ref: ./compression.yaml#/components/schemas/IndexerHealth

    getIndexerSlot:
      name: getIndexerSlot
      description: Returns the latest slot indexed by the compression indexer.
      params: []
      examples:
        - name: getIndexerSlot example
          params: []
      result:
        name: Indexer slot
        description: The latest slot indexed by the compression indexer.
        schema:
          $ref: ./compression.yaml#/components/schemas/UnsignedInteger

    getLatestCompressionSignatures:
      name: getLatestCompressionSignatures
      description: Returns the most recent transaction signatures involving compressed state.
      paramStructure: by-name
      params:
        - name: cursor
          required: false
          description: Pagination cursor returned by a previous response.
          schema:
            oneOf:
              - type: string
              - type: "null"
        - name: limit
          required: false
          description: Maximum number of signatures to return.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Limit
              - type: "null"
      examples:
        - name: getLatestCompressionSignatures example
          params: []
      result:
        name: Signatures
        description: A paginated list of recent compression-related transaction signatures.
        schema:
          $ref: ./compression.yaml#/components/schemas/PaginatedSignatureInfoListResult

    getLatestNonVotingSignatures:
      name: getLatestNonVotingSignatures
      description: Returns the most recent non-voting transaction signatures observed by the indexer.
      paramStructure: by-name
      params:
        - name: cursor
          required: false
          description: Pagination cursor returned by a previous response.
          schema:
            oneOf:
              - type: string
              - type: "null"
        - name: limit
          required: false
          description: Maximum number of signatures to return.
          schema:
            oneOf:
              - $ref: ./compression.yaml#/components/schemas/Limit
              - type: "null"
      examples:
        - name: getLatestNonVotingSignatures example
          params: []
      result:
        name: Signatures
        description: A list of recent non-voting transaction signatures with their error status.
        schema:
          $ref: ./compression.yaml#/components/schemas/SignatureInfoListWithErrorResult

    getMultipleCompressedAccountProofs:
      name: getMultipleCompressedAccountProofs
      description: Returns merkle proofs for the compressed accounts with the given hashes.
      params:
        - name: hashes
          required: true
          description: An array of compressed account hashes to query.
          schema:
            type: array
            items:
              $ref: ./compression.yaml#/components/schemas/Hash
      examples:
        - name: getMultipleCompressedAccountProofs example
          params:
            - name: hashes
              value:
                - "3KC9cKQhhzEPjwuyQaohM6SGaZu1ukKRZw2QRgc1sYVL"
                - "41GMzTte97MK7eZqo5rb7JNjABrm4r4XkhSCVZdZgtka"
      result:
        name: Compressed account proofs
        description: An array of merkle proofs for the requested compressed accounts.
        schema:
          $ref: ./compression.yaml#/components/schemas/AccountProofListResult

    getMultipleCompressedAccounts:
      name: getMultipleCompressedAccounts
      description: Returns the compressed accounts with the given addresses or hashes. Provide either `addresses` or `hashes`.
      paramStructure: by-name
      params:
        - name: addresses
          required: false
          description: An array of compressed account addresses to query.
          schema:
            oneOf:
              - type: array
                items:
                  $ref: ./base-types.yaml#/components/schemas/Pubkey
              - type: "null"
        - name: hashes
          required: false
          description: An array of compressed account hashes to query.
          schema:
            oneOf:
              - type: array
                items:
                  $ref: ./compression.yaml#/components/schemas/Hash
              - type: "null"
      examples:
        - name: getMultipleCompressedAccounts example
          params:
            - name: hashes
              value:
                - "3KC9cKQhhzEPjwuyQaohM6SGaZu1ukKRZw2QRgc1sYVL"
                - "41GMzTte97MK7eZqo5rb7JNjABrm4r4XkhSCVZdZgtka"
      result:
        name: Compressed accounts
        description: A list of compressed accounts.
        schema:
          $ref: ./compression.yaml#/components/schemas/AccountListResult

    getMultipleNewAddressProofs:
      name: getMultipleNewAddressProofs
      description: Returns non-inclusion proofs for the given addresses, used when creating compressed accounts.
      params:
        - name: addresses
          required: true
          description: An array of addresses to prove non-inclusion for.
          schema:
            type: array
            items:
              $ref: ./base-types.yaml#/components/schemas/Pubkey
      examples:
        - name: getMultipleNewAddressProofs example
          params:
            - name: addresses
              value:
                - "11111112cMQwSC9qirWGjZM6gLGwW69X22mqwLLGP"
      result:
        name: New address proofs
        description: An array of non-inclusion proofs for the requested addresses.
        schema:
          $ref: ./compression.yaml#/components/schemas/NewAddressProofListResult

    getMultipleNewAddressProofsV2:
      name: getMultipleNewAddressProofsV2
      description: Returns non-inclusion proofs for the given (address, tree) pairs, used when creating compressed accounts.
      params:
        - name: addressesWithTrees
          required: true
          description: An array of address and tree Pubkey pairs to prove non-inclusion for.
          schema:
            type: array
            items:
              $ref: ./compression.yaml#/components/schemas/AddressWithTree
      examples:
        - name: getMultipleNewAddressProofsV2 example
          params:
            - name: addressesWithTrees
              value:
                - address: "11111112cMQwSC9qirWGjZM6gLGwW69X22mqwLLGP"
                  tree: "11111112cMQwSC9qirWGjZM6gLGwW69X22mqwLLGP"
      result:
        name: New address proofs
        description: An array of non-inclusion proofs for the requested (address, tree) pairs.
        schema:
          $ref: ./compression.yaml#/components/schemas/NewAddressProofListResult

    getTransactionWithCompressionInfo:
      name: getTransactionWithCompressionInfo
      description: Returns the transaction details for the given signature, with additional compression information about accounts opened or closed.
      params:
        - name: signature
          required: true
          description: The transaction signature to query.
          schema:
            $ref: ./compression.yaml#/components/schemas/SerializableSignature
      examples:
        - name: getTransactionWithCompressionInfo example
          params:
            - name: signature
              value: "5J8H5sTvEhnGcB4R8K1n7mfoiWUD9RzPVGES7e3WxC7c"
      result:
        name: Transaction with compression info
        description: The transaction details with associated compression state changes.
        schema:
          $ref: ./compression.yaml#/components/schemas/TransactionWithCompressionInfo

    getValidityProof:
      name: getValidityProof
      description: Returns a single ZK validity proof for the given compressed account hashes and/or new addresses.
      paramStructure: by-name
      params:
        - name: hashes
          required: false
          description: Compressed account hashes to prove inclusion for.
          schema:
            type: array
            items:
              $ref: ./compression.yaml#/components/schemas/Hash
        - name: newAddressesWithTrees
          required: false
          description: New addresses paired with their merkle tree Pubkeys, to prove non-inclusion for.
          schema:
            type: array
            items:
              $ref: ./compression.yaml#/components/schemas/AddressWithTree
      examples:
        - name: getValidityProof example
          params:
            - name: hashes
              value:
                - "3KC9cKQhhzEPjwuyQaohM6SGaZu1ukKRZw2QRgc1sYVL"
            - name: newAddressesWithTrees
              value: []
      result:
        name: Validity proof
        description: A ZK validity proof with context for inclusion and non-inclusion claims.
        schema:
          $ref: ./compression.yaml#/components/schemas/ValidityProofResult

    getValidityProofV2:
      name: getValidityProofV2
      description: Returns a single ZK validity proof for the given compressed account hashes and/or new addresses, with V2 merkle context information.
      paramStructure: by-name
      params:
        - name: hashes
          required: false
          description: Compressed account hashes to prove inclusion for.
          schema:
            type: array
            items:
              $ref: ./compression.yaml#/components/schemas/Hash
        - name: newAddressesWithTrees
          required: false
          description: New addresses paired with their merkle tree Pubkeys, to prove non-inclusion for.
          schema:
            type: array
            items:
              $ref: ./compression.yaml#/components/schemas/AddressWithTree
      examples:
        - name: getValidityProofV2 example
          params:
            - name: hashes
              value:
                - "3KC9cKQhhzEPjwuyQaohM6SGaZu1ukKRZw2QRgc1sYVL"
            - name: newAddressesWithTrees
              value: []
      result:
        name: Validity proof
        description: A ZK validity proof with V2 merkle context for inclusion and non-inclusion claims.
        schema:
          $ref: ./compression.yaml#/components/schemas/ValidityProofResultV2
