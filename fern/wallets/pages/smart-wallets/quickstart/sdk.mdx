---
title: Transaction SDK Quickstart
subtitle: Learn to interact with Wallet APIs using the Wallet Client SDK
url: https://www.alchemy.com/docs/wallets/smart-wallet-quickstart/sdk
slug: wallets/smart-wallet-quickstart/sdk
---

### 1. Install Prerequisites

You're going to need `@alchemy/wallet-apis` and `viem`.

<CodeGroup>
```shell npm
npm install @alchemy/wallet-apis@0.0.0-alpha.19 viem
```

```shell bun
bun add @alchemy/wallet-apis@0.0.0-alpha.19 viem
```

```shell yarn
yarn add @alchemy/wallet-apis@0.0.0-alpha.19 viem
```

```shell pnpm
pnpm add @alchemy/wallet-apis@0.0.0-alpha.19 viem
```

</CodeGroup>

### 2. Create A Smart Account Client

Given a signer (e.g. `privateKeyToAccount` from `viem/accounts`), all you need to do is follow a few simple steps to start sending user ops with Wallet APIs!

```ts
import { createSmartWalletClient, alchemyWalletTransport } from "@alchemy/wallet-apis";
import { arbitrumSepolia } from "viem/chains";
import { privateKeyToAccount } from "viem/accounts";

const signer = privateKeyToAccount(PRIVATE_KEY); // we use a private key signer as an example here

const client = createSmartWalletClient({
  transport: alchemyWalletTransport({
    apiKey: ALCHEMY_API_KEY,
  }),
  chain: arbitrumSepolia,
  signer,
  paymaster: {
    policyId: GAS_MANAGER_POLICY_ID,
  },
});
```

### 3. Send A UserOp

The client defaults to [EIP-7702](/wallets/transactions/using-eip-7702). This first transaction will delegate your account.

```ts
import { zeroAddress } from "viem";

const { id } = await client.sendCalls({
  calls: [{ to: zeroAddress, value: BigInt(0) }],
});

const status = await client.getCallsStatus(id);
```

<Accordion title="Using @account-kit/wallet-client (v4)?">
  <Markdown src="../../../shared/v4-client-note.mdx" />
  <Markdown src="../../../shared/v4-paymaster-note.mdx" />
</Accordion>
