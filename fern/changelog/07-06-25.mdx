## Summary

In the latest release, we've added a new endpoint called `linea_estimateGas`

### What's new?

New endpoints:

* `POST /linea_estimateGas` estimate gas for a transaction on Linea.

New object schemas:

* `LineaEstimateGasRequest`
* `LineaEstimateGasResponse`
