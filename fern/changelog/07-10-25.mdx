## Summary

In the latest release, we've updated Bundler API Endpoints.

### What's new?

New endpoints:

* `POST /bundler/v1/estimateGas` estimate gas for a transaction on all chains.

New object schemas:

* `EstimateGasRequest`
* `EstimateGasResponse`
