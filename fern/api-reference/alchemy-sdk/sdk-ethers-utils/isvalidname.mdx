---
title: isValidName
description: Learn about the isValidName utils method
subtitle: Learn about the isValidName utils method
url: https://docs.alchemy.com/reference/isvalidname
slug: reference/isvalidname
---

Learn about the `isValidName` utils method

# Introduction

The [`isValidName`](https://docs.ethers.org/v4/api-utils.html) method is used to check if a string is a valid Ethereum name. This method is often used to validate Ethereum names (ENS).

# Usage

This method should be used when checking if a string is a valid Ethereum name. For example, you can use this method to check if the string `'myName'` is a valid Ethereum name. The result would be `true`.

Here's a simple code example to demonstrate how to use this method:

<CodeGroup>
  ```javascript Javascript
  const { Utils } = require("alchemy-sdk");

  let string = "myName";
  let isValid = Utils.isValidName(string); 
  console.log(isValid); // true
  ```
</CodeGroup>
