---
title: "l4BookUpdates stream"
description: "HyperCore private-preview documentation."
---

<Markdown src="../../shared/preview-notice-september.mdx" />

# l4BookUpdates stream

For the corresponding gRPC update stream, see [StreamL4BookUpdates](../../hypercore-grpc/stream-l4-book-updates.mdx). For the snapshot-to-diff recovery pattern, see [l2BookDiff](l2-book-diff.mdx#snapshot-to-diff-bootstrap).

## Subscribe
> The values below illustrate the intended request shape.

```json
{
  "method": "subscribe",
  "subscription": {
    "type": "l4BookUpdates",
    "coins": [
      "BTC",
      "ETH"
    ]
  }
}
```

## Acknowledgement
```json
{
  "channel": "subscriptionResponse",
  "data": {
    "subscriptionId": "sub_01",
    "type": "l4BookUpdates"
  }
}
```

## Event envelope

```json
{
  "channel": "l4BookUpdates",
  "subscriptionId": "sub_01",
  "blockHeight": 123456,
  "blockTime": 1780000000000,
  "checkpoint": "<opaque checkpoint>",
  "data": {
    "coin": "BTC",
    "orders": [
      {
        "orderId": "12345",
        "px": "100000.0",
        "sz": "0.10",
        "action": "upsert"
      }
    ]
  }
}
```

## Filters and behavior

* `coins` is an array of market symbols. One subscription can cover several markets. Omitting `coins` requests the stream's default market-wide scope.
* Supply `checkpoint` in the subscription object to request replay after that checkpoint. `cursor` is an accepted alias for `checkpoint` both when it is received in a message and when it is supplied for resumption. The checkpoint is opaque: clients store and return it unmodified, and its format may change.

## Payload fields

The payload shape is proposed.

| Field | Type | Description |
| --- | --- | --- |
| coin | string | Market identifier. |
| orders | array | Order records. |
| orders[] | object | Order records. |
| orders[].orderId | string | Illustrative orderId value. |
| orders[].px | string | Price value. |
| orders[].sz | string | Size value. |
| orders[].action | string | Illustrative action value. |
## Unsubscribe
```json
{
  "method": "unsubscribe",
  "subscription": {
    "type": "l4BookUpdates",
    "coins": [
      "BTC",
      "ETH"
    ]
  }
}
```
## Resume

To resume a stream, supply `checkpoint` in the subscription object. `cursor` is an accepted alias for `checkpoint` both when it is received in a message and when it is supplied for resumption. The checkpoint is opaque: clients store and return it unmodified, and its format may change.
