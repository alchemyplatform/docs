In v4, the paymaster capability is called `paymasterService`:

```ts
// v4
capabilities: {
  paymasterService: {
    policyId: "your-policy-id",
  },
}
```

Or at the client level, v4 uses a top-level `policyId` instead of `paymaster: { policyId }`.
