---
title: Alchemy for Agents
description: Authenticate your AI agent with a crypto wallet and pay for Alchemy API requests with USDC using the x402 protocol.
subtitle: Authenticate your AI agent with a crypto wallet and pay for Alchemy API requests with USDC using the x402 protocol.
slug: docs/alchemy-for-agents
---

Authenticate your AI agent with a crypto wallet instead of an API key. Your agent pays per request with USDC via the [x402 payment protocol](https://www.x402.org/), with no human in the loop. We support EVM wallets (SIWE) and Solana wallets (SIWS).

## How it works

x402 wallet authentication uses three open standards:

- **[SIWE (Sign-In With Ethereum)](https://eips.ethereum.org/EIPS/eip-4361):** Your agent authenticates by signing a message with an EVM wallet. Pays with USDC on Base.
- **SIWS (Sign-In With Solana):** Adapted from EIP-4361 for Solana's ed25519 signatures. Your agent authenticates with a Solana wallet. Pays with USDC on Solana.
- **[x402](https://www.x402.org/):** An open protocol for HTTP-native payments. When a request requires payment, the server responds with HTTP 402, your agent pays with USDC, and retries the request. Zero protocol fees.

## The flow

When your agent needs to access Alchemy APIs without an API key, it handles the full wallet authentication flow:

1. **Wallet setup:** Your agent creates a new wallet or imports an existing one. You choose between an EVM wallet (pays USDC on Base) or a Solana wallet (pays USDC on Solana).
2. **Authentication:** Your agent generates a signed auth token using SIWE (EVM) or SIWS (Solana) and includes it in every API request.
3. **Payments:** If a request requires payment, the server responds with HTTP 402. Your agent pays with USDC and retries automatically.

Your wallet type only determines authentication and payment. Either wallet type can query any chain, including Ethereum, Solana, Base, Arbitrum, and 100+ others.

## What your agent can access

All standard Alchemy APIs are available through the x402 gateway:

- **Node RPC:** Standard Ethereum JSON-RPC methods (`eth_*`) plus Alchemy-enhanced methods, across 100+ chains
- **Token API:** Token balances, metadata, and allowances
- **NFT API:** NFT ownership, metadata, sales, and spam detection
- **Transfers API:** Asset transfer history with filtering
- **Prices API:** Spot and historical token prices
- **Portfolio API:** Multi-chain balances and portfolio data
- **Simulation API:** Transaction simulation and outcome prediction

These are the same APIs available with a standard API key. The only difference is the authentication and payment mechanism.

## EVM vs Solana wallets

| | EVM wallet | Solana wallet |
|---|---|---|
| **Standard** | SIWE (EIP-4361) | SIWS |
| **Signature** | secp256k1 | ed25519 |
| **Payment** | USDC on Base | USDC on Solana |
| **Queryable chains** | Any (100+) | Any (100+) |

## Install as an agent skill

Install the `agentic-gateway` [agent skill](docs/alchemy-agent-skills) so your AI coding agent learns this flow automatically:

```bash
npx skills add alchemyplatform/skills --yes
```

Your agent will walk you through wallet setup and handle authentication and payments from there.

## Next steps

- [agents.alchemy.com](https://agents.alchemy.com/)
- [x402.org](https://www.x402.org/)
- [EIP-4361: SIWE](https://eips.ethereum.org/EIPS/eip-4361)
- [Agent Skills](docs/alchemy-agent-skills)
