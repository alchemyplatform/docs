apis:
  accounts:
    root: src/openapi/accounts.yaml
  blocks:
    root: src/openapi/blocks.yaml
  nft:
    root: src/openapi/nft.yaml
  notify:
    root: src/openapi/notify.yaml
  prices:
    root: src/openapi/prices.yaml
  transactions:
    root: src/openapi/transactions.yaml
extends:
  - recommended
theme:
  openapi: {}
rules:
  "security-defined": off # APIs use API key in the URL path, not headers/query/cookie
